version: '3.8'

services:

  react:
    labels:
      shipyard.route: '/'
      args:
        REACT_APP_BUILD_ARG: 'static docker-compose build arg'
    build:
      context: .
    volumes:
      - './src:/app/src'
      - './public:/app/public'
    ports:
      - '5000:5000'
